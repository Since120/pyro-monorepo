{
    "name": "pyro-prisma",
    "version": "1.0.0",
    "private": true,
    "scripts": {
    "dev:migrate": "prisma migrate dev",
    "migrate:deploy": "prisma migrate deploy",
    "dev:generate": "prisma generate dev",
    "studio": "prisma studio --schema ./schema.prisma"

  },
  "devDependencies": {
    "prisma": "^6.3.0"
  },
  "dependencies": {
    "@prisma/client": "^6.3.0"
  }
}
