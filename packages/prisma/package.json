{
    "name": "pyro-prisma",
    "version": "1.0.0",
    "private": true,
    "scripts": {
    "migrate:dev": "prisma migrate dev",
    "migrate:deploy": "prisma migrate deploy",
    "generate:dev": "prisma generate dev",
    "studio": "prisma studio --schema ./schema.prisma"

  },
  "devDependencies": {
    "prisma": "^6.2.1"
  },
  "dependencies": {
    "@prisma/client": "^6.2.1"
  }
}
